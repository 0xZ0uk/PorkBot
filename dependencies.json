{
  "version": 1,
  "packages": [
    {
      "name": "@earendil-works/pi-agent-core",
      "version": "0.85.1",
      "reason": "Pi owns the agent loop (PRD decisions 13 and 31) and is adapted at the RunSession seam in slice 5.3. Pinned exactly so an upgrade is a reviewed change paired with the golden-corpus replay, never a silent lockfile move."
    },
    {
      "name": "@hono/node-server",
      "version": "2.1.1",
      "reason": "The Node adapter between the API's fetch surface and node:http (slice 4.1). Pinned exactly because it owns how every request and response crosses that boundary, so an implicit upgrade is a change to request handling nobody reviewed."
    },
    {
      "name": "@orpc/client",
      "version": "1.15.1",
      "reason": "The typed client and RPC link the contract's consumers call through (slice 4.1, PRD decisions 14 and 15). Pinned exactly because it owns the wire encoding on the client side; an implicit upgrade can change what a deployed client sends."
    },
    {
      "name": "@orpc/contract",
      "version": "1.15.1",
      "reason": "The contract builder every procedure in packages/contracts is defined with (slice 4.1, PRD decisions 14 and 15). Pinned exactly because the contract is the single source of transport truth: a version move changes procedure, error and wire semantics for both client and server at once."
    },
    {
      "name": "@orpc/openapi",
      "version": "1.15.1",
      "reason": "Generates the OpenAPI document from the same contract the router implements, asserted in the unit suite (slice 4.1, PRD decision 14). Pinned exactly because the generator decides the published document's shape, which is the promise the contract-first design makes."
    },
    {
      "name": "@orpc/server",
      "version": "1.15.1",
      "reason": "Implements the contract, matches RPC requests and turns procedure errors into responses (slice 4.1, PRD decisions 14 and 28). Pinned exactly because it owns the server side of the wire protocol and the error envelope clients parse."
    },
    {
      "name": "@orpc/zod",
      "version": "1.15.1",
      "reason": "Converts the contract's Zod schemas into JSON Schema for the OpenAPI document (slice 4.1). Pinned exactly because the conversion decides the document's schema output, and it must agree with the validation the same schemas perform at runtime."
    },
    {
      "name": "better-auth",
      "version": "1.7.5",
      "reason": "Email/password, sessions and verification for the one auth gate (slice 3.1, PRD decision 8). Pinned exactly because it owns password hashing, session-cookie issuance and origin checks, where an implicit upgrade is a security change nobody reviewed; the identity schema was generated for this library's table shape."
    },
    {
      "name": "effect",
      "version": "3.22.2",
      "reason": "The orchestration layer (PRD decision 12): provider services and their layers, retries, timeouts, resource safety, structured concurrency and typed error channels. Pinned exactly because its Cause, Layer and Context semantics are the error-mapping and lifetime contract every later service is written against; the major is chosen deliberately (3.x stable, not the 4.x release candidate)."
    },
    {
      "name": "graphile-worker",
      "version": "0.18.0",
      "reason": "The durable job runner the worker boots (slice 6.1, PRD decision 17): job locking, retries and its own queue schema in Postgres. Pinned exactly because it migrates that schema at boot, so an implicit upgrade would silently change the queue's tables and job semantics on the production database."
    },
    {
      "name": "hono",
      "version": "4.13.8",
      "reason": "The API's HTTP surface (slice 4.1, PRD decision 14: oRPC over Hono), mounted through the RPC handler and composed by every later route, stream and webhook. Pinned exactly because routing, middleware order and header handling are user-visible behaviors an implicit upgrade could move."
    },
    {
      "name": "varlock",
      "version": "1.20.0",
      "reason": "The environment schema and audit CLI (varlock.dev) the root and per-package .env.schema files are written against, and the `env` CI tier runs. Pinned exactly because a schema is only a contract while the tool that reads it does not move underneath it: an upgrade can change decorator parsing, validation and `varlock audit` behavior at once."
    },
    {
      "name": "zod",
      "version": "4.6.5",
      "reason": "The schema library every contract procedure is declared with and the one @orpc/zod converts into the OpenAPI document (slice 4.1, PRD decision 15). Pinned exactly because the same schemas validate requests at runtime and describe them publicly, so a move changes both validation behavior and the published document."
    }
  ],
  "images": [
    {
      "name": "node",
      "reference": "node:24.21.0-bookworm-slim@sha256:2fe369e969550cde8e867afc3fe370b260140cab4a23d467074295b42163d553",
      "reason": "The build and runtime base for every stage of the root Dockerfile. The tag carries the exact Node version from .nvmrc, which pnpm's devEngines check refuses to substitute, and the digest keeps the stack on the image it was built and tested with."
    },
    {
      "name": "postgres",
      "reference": "postgres:18@sha256:4ef4dbc939d61acea57712655ddb4b4ab27419c913f94cca0cd57cb3ea3c2280",
      "reason": "The production major (PRD stack decision 11). The testkit harness boots it and the integration tier pulls it; the digest keeps both on the image the tier was proven against."
    }
  ]
}
