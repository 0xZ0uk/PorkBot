export const moduleInfo = {
  name: "@porkbot/desktop",
  summary:
    "Connect-only Electron shell: the packaged web build on a loopback origin, the API behind a proxy, hardened windows, a tray and signed updates.",
} as const;
