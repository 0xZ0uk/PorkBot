import { colors } from "@porkbot/tokens";

// The legal version of ui-hardcoded-color.ts: the same card, drawn from the
// semantic tokens. This fixture catches a colour rule that fires on the tokens
// it is supposed to encourage.
export const card = {
  background: colors.surface,
  borderColor: colors.border,
};
