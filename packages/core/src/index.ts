export const moduleInfo = {
  name: "@porkbot/core",
  summary:
    "Pure domain rules: run state machine, event reducer, policies. No I/O, no frameworks, no database drivers.",
} as const;

// PROOF ONLY — reverted in the next commit.
export const deliberateBuildBreak: number = "a string is not a number";
