# Committed fixtures for `APP_ENV=ci` only (see .env.schema). The `env` CI tier
# and `pnpm env:check` load these to prove every schema resolves; the values are
# obvious fakes and no real deployment or test ever reads them. Vite never
# loads `.env.ci`, so no Vitest process sees them either.
DATABASE_URL=postgres://porkbot:ci-fixture-password-placeholder@127.0.0.1:5432/porkbot_ci
